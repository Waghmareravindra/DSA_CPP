#include <iostream>
#include <string.h>

using namespace std;



int main()
{

  // // Char AND Array Difference AND Called

  // char arr[]={'a','b','c','d','e'};
  // char str[]="hello"; std::string ravindra="hallow diamond";

  // // cout<<arr[3];
  // cout<<ravindra;

  // // leet code question remove all adjacent duplicates in string

/*it will run on leet code only if we want to run on vs code then you need to string the string */


//     class Solution {
// public:
//     string removeDuplicates(string s) {
//         string ans ="";
//         int i = 0;

//         while( i < s.length() ) {

//                 if( ans.length() > 0  &&  ans[ans.length() - 1] == s[i]) {
//                     ans.pop_back();
//                 }
//                 else {
//                     ans.push_back(s[i]);
//                 }
//             i++;
//         }
//         return ans;
//     }
// };

}
